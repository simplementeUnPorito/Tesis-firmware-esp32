[env:esp32dev]
platform  = espressif32
board     = esp32dev
framework = arduino

upload_port  = COM8       ; <-- ajustar al COM real del maestro
monitor_port = COM8
monitor_speed = 921600

build_flags =
    -DCORE_DEBUG_LEVEL=3
    -DCONFIG_ARDUINO_LOOP_STACK_SIZE=8192
    ; Mantener en 0 cuando MATLAB usa el mismo puerto serial binario
    -DMASTER_SERIAL_LOG=0
    ; IMU del martillo: 0=stub (sin hardware), 1=real (habilitar driver en hammer_imu.h)
    -DHAMMER_IMU_ENABLED=0
    ; Número de nodos esclavos esperados
    -DNUM_SLAVES=3
    ; Deshabilitado: no queremos pulsos por muestra en el osciloscopio
    -DSAMPLE_PULSE_PIN=-1
    -DSAMPLE_PULSE_IDLE=0
    -DSAMPLE_PULSE_US=20
    ; Marcador unico de START para osciloscopio
    -DDEBUG_HARDWARE=1
    -DDEBUG_HW_START_PIN=15
    -DDEBUG_HW_START_IDLE=0
    ; Ancho del pulso START (maestro y esclavos). Cambiar tambien en slave/platformio.ini.
    -DSCOPE_START_PULSE_US=1000
    ; Espera fija antes de cada START real o START falso multiple.
    -DPRESTART_WAIT_MS=5000
    ; Cantidad y pausa extra entre PRESTARTs del modo "Starts multiples".
    ; Periodo entre pulsos ~= PRESTART_WAIT_MS + SCOPE_MULTI_START_GAP_MS.
    -DSCOPE_MULTI_START_COUNT=128
    -DSCOPE_MULTI_START_GAP_MS=1000
